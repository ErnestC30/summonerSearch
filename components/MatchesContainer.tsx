import { SummonerDTO } from "../interfaces";
import Match from "./Match";


export default function MatchesContainer(summonerData: SummonerDTO, region: string) {

    //load matches based off summonerdata

    return (
        <div>hello</div>
    )
}